<?php
/**
 * secrets.php.example — copie para  agente-sgp-php/secrets.php  e preencha.
 * O secrets.php real não vai pro git. As MESMAS credenciais MYSQL_* do backend web.
 * (Um .env no mesmo diretório também é aceito.)
 */

return [
    'MYSQL_HOST'     => 'mysql-svc.database.svc.cluster.local',
    'MYSQL_PORT'     => '3306',
    'MYSQL_USER'     => 'dashboard_40bpmi',
    'MYSQL_PASSWORD' => '',
    'MYSQL_DATABASE' => 'dashboard_40bpmi',

    // Intervalo entre verificações da fila (ms) — sem pressa, não é tempo real.
    'POLL_INTERVAL_MS' => '60000',
    // Intervalo entre chamadas ao WSSCPM/SGP-DP num lote (ms).
    'CALL_DELAY_MS' => '1500',

    // Caminho do certificado da CA interna do SGP-DP (só p/ IAS/cursos/láureas).
    // Padrão: agente-sgp-php/certs/sgp-dp-ca.pem
    // 'SGPDP_CA_CERT_PATH' => '/etc/ssl/sgp-dp-ca.pem',
];
